# Client environment variables (for production deployment)
# In development, the Vite proxy handles /api requests automatically.
# In production, set this to your deployed server URL (without trailing slash).

# Example for Render deployment:
# VITE_API_BASE_URL=https://slidesmaker-server.onrender.com

# Example for Railway deployment:
# VITE_API_BASE_URL=https://slidesmaker-production.up.railway.app

# Leave empty or omit for local development (uses Vite proxy)
VITE_API_BASE_URL=
